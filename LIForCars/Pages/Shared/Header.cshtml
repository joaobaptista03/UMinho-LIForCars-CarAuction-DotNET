@page
@model HeaderModel

<header>
    <h2>LIFORCARS</h2>
    <nav>
        <a href="/">HOME</a>
        @if (User.Identity != null && User.Identity.IsAuthenticated) {
            <a href="/Auctions">AUCTIONS</a> 
            <a href="/UserPage">PROFILE</a>
            <button type="submit" class="signout-button">LOG OUT</button>
        } else {
            <a href="#" onclick="showAboutPage()">ABOUT</a>
            <a href="#" onclick="showContactPage()">CONTACT</a>
            <divSignIn>
                <a2 href="#" onclick="showLoginPage()" class="signin">SIGN IN</a2>
            </divSignIn>
        }
    </nav>
</header>